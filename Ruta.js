export default class Ruta{
  constructor(ruta){
    this._baseInicio = ruta.baseInicio;
    this._horaInicio = ruta.horaInicio;
    this._horaFin = ruta.horaFin;
  }
  crearRecorrido(){
    //baseInicio, horaInicio, horaFin
  }

  //if()
}
